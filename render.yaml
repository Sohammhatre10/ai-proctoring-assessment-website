services:
  - type: web
    name: ai-proctoring-backend
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
    buildFilter:
      paths:
        - backend/**
